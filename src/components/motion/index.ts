// Re-export Motion components for easy access
export {
  motion,
  AnimatePresence,
  useAnimation,
  useInView,
  useScroll,
  useTransform,
  useSpring,
  useMotionValue,
  useMotionValueEvent,
} from 'motion/react'

// Re-export animation presets
export * from '@/lib/animations'
